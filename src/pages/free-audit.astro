---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
import Footer from '../components/Footer.astro';

// Page content
const content = {
  title: "Get Your Free Epoxy Lead Audit",
  seo: {
    metaTitle: "Free Epoxy Lead Audit | Find Your Lost Leads - Floorplay Agency",
    metaDescription: "Get a free audit showing exactly how many $5k+ epoxy jobs you're losing to competitors online. See where your leads are going and how to get them back."
  },
  hero: {
    headline: "Get Your Free Epoxy Lead Audit",
    subheadline: "See exactly how many $5k+ jobs you're losing to competitors online and get a personalized action plan to capture them.",
    benefits: [
      "Competitor analysis for your local market",
      "Search ranking gaps costing you leads", 
      "Custom strategy to dominate local searches"
    ]
  }
};

// Image constants (reusing from homepage)
const imgDivider = "http://localhost:3845/assets/5234c6adb3e005f5c06592e41b6fc5cb71a97964.svg";
---

<Layout 
  title={content.seo?.metaTitle || content.title}
  description={content.seo?.metaDescription}
  canonical="https://floorplay.agency/free-audit/"
>

  <!-- Header Section -->
  <section class="header-section">
    <div class="container">
      <Navigation />
    </div>
  </section>

  <!-- Form Section -->
  <section class="form-section">
    <div class="container">
      <!-- Header Content -->
      <div class="form-header">
        <h1 class="form-headline">
          Get Your <span class="highlight-blue">Free</span> Local Search Plan
        </h1>
        <p class="form-subheadline">
          See exactly how many $5k+ jobs you're losing to competitors online and get a personalized action plan to capture them.
        </p>
        <div class="benefits-list">
          {content.hero.benefits.map((benefit) => (
            <div class="benefit-item">
              <span class="benefit-check">✓</span>
              <span>{benefit}</span>
            </div>
          ))}
        </div>
      </div>

      <!-- Audit Form -->
      <div class="audit-form">
                <form id="auditForm" name="audit-request" method="POST" netlify action="/thank-you">
          <input type="hidden" name="form-name" value="audit-request" />
          <div style="display: none;">
            <label>Don't fill this out if you're human: <input name="bot-field" /></label>
          </div>
          <div class="form-grid">
            <div class="form-group">
              <label for="firstName" class="form-label">First Name *</label>
              <input 
                type="text" 
                id="firstName" 
                name="firstName" 
                class="form-input" 
                required 
                placeholder="Enter your first name"
              />
            </div>

            <div class="form-group">
              <label for="lastName" class="form-label">Last Name *</label>
              <input 
                type="text" 
                id="lastName" 
                name="lastName" 
                class="form-input" 
                required 
                placeholder="Enter your last name"
              />
            </div>

            <div class="form-group">
              <label for="email" class="form-label">Business Email *</label>
              <input 
                type="email" 
                id="email" 
                name="email" 
                class="form-input" 
                required 
                placeholder="your.email@company.com"
              />
            </div>

            <div class="form-group">
              <label for="phone" class="form-label">Phone Number *</label>
              <input 
                type="tel" 
                id="phone" 
                name="phone" 
                class="form-input" 
                required 
                placeholder="(555) 123-4567"
              />
            </div>

            <div class="form-group">
              <label for="company" class="form-label">Company Name *</label>
              <input 
                type="text" 
                id="company" 
                name="company" 
                class="form-input" 
                required 
                placeholder="Your Company Name"
              />
            </div>

            <div class="form-group">
              <label for="website" class="form-label">Website URL</label>
              <input 
                type="url" 
                id="website" 
                name="website" 
                class="form-input" 
                placeholder="https://yourwebsite.com"
              />
            </div>

            <div class="form-group full-width">
              <label for="city" class="form-label">Primary Service Area (City, State) *</label>
              <input 
                type="text" 
                id="city" 
                name="city" 
                class="form-input" 
                required 
                placeholder="e.g., Mequon, WI"
              />
            </div>

            <div class="form-group full-width">
              <label for="services" class="form-label">Primary Services *</label>
              <div class="checkbox-group">
                <label class="checkbox-item">
                  <input type="checkbox" name="services" value="garage-epoxy" />
                  <span class="checkmark"></span>
                  Garage Epoxy Flooring
                </label>
                <label class="checkbox-item">
                  <input type="checkbox" name="services" value="basement-flooring" />
                  <span class="checkmark"></span>
                  Basement Flooring
                </label>
                <label class="checkbox-item">
                  <input type="checkbox" name="services" value="commercial-epoxy" />
                  <span class="checkmark"></span>
                  Commercial Epoxy
                </label>
                <label class="checkbox-item">
                  <input type="checkbox" name="services" value="concrete-coating" />
                  <span class="checkmark"></span>
                  Concrete Coatings
                </label>
              </div>
            </div>

            <div class="form-group full-width">
              <label for="monthlyJobs" class="form-label">How many epoxy jobs do you complete monthly? *</label>
              <select id="monthlyJobs" name="monthlyJobs" class="form-select" required>
                <option value="">Select range...</option>
                <option value="0-2">0-2 jobs</option>
                <option value="3-5">3-5 jobs</option>
                <option value="6-10">6-10 jobs</option>
                <option value="11-20">11-20 jobs</option>
                <option value="20+">20+ jobs</option>
              </select>
            </div>

            <div class="form-group full-width">
              <label for="challenges" class="form-label">Biggest lead generation challenge? *</label>
              <select id="challenges" name="challenges" class="form-select" required>
                <option value="">Select your biggest challenge...</option>
                <option value="not-enough-leads">Not getting enough leads</option>
                <option value="low-quality-leads">Getting low-quality leads</option>
                <option value="losing-to-competitors">Losing jobs to competitors</option>
                <option value="poor-online-presence">Poor online visibility</option>
                <option value="pricing-pressure">Pricing pressure</option>
                <option value="seasonal-fluctuations">Seasonal business fluctuations</option>
              </select>
            </div>
          </div>

          <div class="form-submit">
            <button type="submit" class="submit-button">
              <span class="submit-text">Get My Free Audit Report</span>
              <span class="submit-arrow">→</span>
            </button>
            <p class="form-disclaimer">
              * We'll email your personalized audit within 24 hours. No spam, just actionable insights.
            </p>
          </div>
        </form>
      </div>
    </div>
  </section>



  <Footer />
</Layout>

<style>
  /* Global list styling - best practices */
  ul, ol {
    margin: 0 0 1.5rem 0;
    padding-left: 1.5rem;
  }

  ul:last-child, ol:last-child {
    margin-bottom: 0;
  }

  /* Additional spacing for list items */
  ul li, ol li {
    margin-bottom: 1rem;
  }

  ul li:last-child, ol li:last-child {
    margin-bottom: 0;
  }

  /* Portable Text list styling */
  .portable-text ul, .portable-text ol,
  [data-portable-text] ul, [data-portable-text] ol {
    margin: 0 0 2rem 0;
    padding-left: 1.5rem;
  }

  .portable-text ul li, .portable-text ol li,
  [data-portable-text] ul li, [data-portable-text] ol li {
    margin-bottom: 1rem;
    line-height: 1.6;
  }

  /* Container */
  .container {
    max-width: 800px;
    margin: 0 auto;
    padding: 0 1rem;
  }

  /* Header Section */
  .header-section {
    background: #ffffff;
    padding: 72px 0;
    position: relative;
    z-index: 1;
  }

  .header {
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .logo-nav {
    display: flex;
    justify-content: space-between;
    align-items: end;
  }

  .logo {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .logo-text {
    font-family: 'Big Shoulders Display', sans-serif;
    font-weight: 700;
    font-size: 32px;
    letter-spacing: 1.6px;
    margin: 0;
    line-height: 0.831;
    color: #000000;
  }

  .logo a {
    text-decoration: none;
    color: inherit;
  }

  .logo-tagline {
    font-family: 'Work Sans', sans-serif;
    font-weight: 400;
    font-size: 14px;
    color: #626262;
    letter-spacing: -0.28px;
    line-height: 1.2;
    text-transform: uppercase;
    margin: 0;
  }

  .nav {
    display: flex;
    gap: 24px;
    align-items: center;
  }

  .nav-link {
    font-family: 'Work Sans', sans-serif;
    font-weight: 600;
    font-size: 16px;
    color: #000000;
    text-decoration: none;
    letter-spacing: 0.8px;
    line-height: 0.831;
    padding: 8px 4px;
    position: relative;
    cursor: pointer;
  }

  .nav-link.active {
    color: #2563eb;
    border-bottom: 2px solid #2563eb;
  }

  .nav-link:hover {
    color: #2563eb;
  }

  .services-link:hover {
    color: #2563eb;
  }

  .nav-cta {
    display: flex;
    align-items: center;
    background: #1e293b;
    color: #ffffff;
    padding: 0;
    border-radius: 4px;
    overflow: hidden;
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s ease;
  }

  .nav-cta.active {
    background: #1e293b;
    border-bottom: none;
  }

  .nav-cta-text {
    padding: 12px 16px;
    background: inherit;
    color: #ffffff;
  }

  .nav-cta-arrow {
    padding: 12px 16px;
    background: #1e293b;
    color: #ffffff;
    display: flex;
    align-items: center;
    justify-content: center;
    min-width: 44px;
  }

  .nav-cta:hover {
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(37, 99, 235, 0.3);
  }

  .divider {
    height: 1px;
    background: url('http://localhost:3845/assets/5234c6adb3e005f5c06592e41b6fc5cb71a97964.svg') repeat-x;
    width: 100%;
  }

  .tagline {
    text-align: right;
  }

  .tagline p {
    font-family: 'Work Sans', sans-serif;
    font-weight: 400;
    font-size: 16px;
    color: #000000;
    letter-spacing: -0.32px;
    line-height: 1.3;
    max-width: 250px;
    margin: 0 0 0 auto;
  }

  /* Form Section */
  .form-section {
    background: #ffffff;
    padding: 40px 0 80px;
    position: relative;
    z-index: 1;
  }

  .form-header {
    text-align: center;
    margin-bottom: 60px;
  }

  .form-headline {
    font-family: 'Big Shoulders Display', sans-serif;
    font-weight: 700;
    font-size: 64px;
    color: #1a1309;
    letter-spacing: -1.28px;
    line-height: 1.1;
    margin: 0 0 24px 0;
    text-wrap: balance;
    text-rendering: optimizeLegibility;
    text-transform: uppercase;
  }

  .highlight-blue {
    color: #0040ff;
  }

  .form-subheadline {
    font-family: 'Work Sans', sans-serif;
    font-weight: 400;
    font-size: 24px;
    color: #000000;
    letter-spacing: -0.48px;
    line-height: 1.3;
    margin: 0 0 32px 0;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
    text-wrap: pretty;
    text-rendering: optimizeLegibility;
  }

  .benefits-list {
    display: flex;
    flex-direction: column;
    gap: 16px;
    max-width: 596px;
    margin: 0 auto;
  }

  .benefit-item {
    display: flex;
    align-items: flex-start;
    gap: 8px;
    font-family: 'Work Sans', sans-serif;
    font-weight: 400;
    font-size: 20px;
    color: #000000;
    letter-spacing: -0.4px;
    line-height: 1.5;
  }

  .benefit-check {
    width: 32px;
    height: 32px;
    background: #000000;
    border-radius: 0;
    color: #ffffff;
    font-weight: 700;
    font-size: 14px;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-top: 0;
  }

  /* Form Styles */
  .audit-form {
    background: #f8f9fa;
    padding: 72px 64px;
    border-radius: 24px;
    border: 4px solid #000000;
  }

  .form-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24px;
    margin-bottom: 40px;
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .form-group.full-width {
    grid-column: 1 / -1;
  }

  .form-label {
    font-family: 'Work Sans', sans-serif;
    font-weight: 600;
    font-size: 16px;
    color: #000000;
    letter-spacing: -0.32px;
    line-height: 1.3;
  }

  .form-input, .form-select {
    padding: 16px 20px;
    border: 2px solid #e5e7eb;
    border-radius: 4px;
    font-family: 'Work Sans', sans-serif;
    font-weight: 400;
    font-size: 16px;
    color: #000000;
    background: #ffffff;
    transition: border-color 0.3s ease;
  }

  .form-input:focus, .form-select:focus {
    outline: none;
    border-color: #2563eb;
  }

  .form-input::placeholder {
    color: #9ca3af;
  }

  .checkbox-group {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
  }

  .checkbox-item {
    display: flex;
    align-items: center;
    gap: 12px;
    cursor: pointer;
    font-family: 'Work Sans', sans-serif;
    font-weight: 400;
    font-size: 16px;
    color: #000000;
    letter-spacing: -0.32px;
    line-height: 1.3;
  }

  .checkbox-item input[type="checkbox"] {
    display: none;
  }

  .checkmark {
    width: 20px;
    height: 20px;
    border: 2px solid #e5e7eb;
    border-radius: 3px;
    background: #ffffff;
    position: relative;
    transition: all 0.3s ease;
  }

  .checkbox-item input[type="checkbox"]:checked + .checkmark {
    background: #2563eb;
    border-color: #2563eb;
  }

  .checkbox-item input[type="checkbox"]:checked + .checkmark::after {
    content: "✓";
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: #ffffff;
    font-weight: 700;
    font-size: 14px;
  }

  .form-submit {
    text-align: center;
  }

  .submit-button {
    background: #2563eb;
    color: #ffffff;
    padding: 0;
    border: none;
    border-radius: 4px;
    font-family: 'Work Sans', sans-serif;
    font-weight: 700;
    font-size: 20px;
    letter-spacing: -0.4px;
    line-height: 1.3;
    cursor: pointer;
    transition: all 0.3s ease;
    margin-bottom: 16px;
    display: inline-flex;
    align-items: center;
    overflow: hidden;
  }

  .submit-text {
    padding: 24px 32px;
    background: #2563eb;
    color: #ffffff;
  }

  .submit-arrow {
    padding: 24px 32px;
    background: #1e293b;
    color: #ffffff;
    display: flex;
    align-items: center;
    justify-content: center;
    min-width: 60px;
  }

  .submit-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(37, 99, 235, 0.3);
  }

  .form-disclaimer {
    font-family: 'Work Sans', sans-serif;
    font-weight: 400;
    font-size: 14px;
    color: #6b7280;
    letter-spacing: -0.28px;
    line-height: 1.3;
    margin: 0;
    max-width: 400px;
    margin-left: auto;
    margin-right: auto;
  }

  /* Additional Content Section */
  .additional-content {
    background: #ffffff;
    padding: 60px 0;
  }

  .content-blocks {
    font-family: 'Work Sans', sans-serif;
    font-weight: 400;
    font-size: 18px;
    color: #000000;
    letter-spacing: -0.36px;
    line-height: 1.4;
  }

  .content-blocks h2 {
    font-family: 'Big Shoulders Display', sans-serif;
    font-weight: 700;
    font-size: 48px;
    color: #000000;
    letter-spacing: -0.96px;
    line-height: 1.3;
    margin: 0 0 24px 0;
    text-wrap: balance;
  }

  .content-blocks h3 {
    font-family: 'Big Shoulders Display', sans-serif;
    font-weight: 700;
    font-size: 32px;
    color: #000000;
    letter-spacing: -0.64px;
    line-height: 1.3;
    margin: 32px 0 16px 0;
    text-wrap: balance;
  }

  .content-blocks p {
    margin: 0 0 16px 0;
  }

  .content-blocks ul,
  .content-blocks ol {
    margin: 16px 0;
    padding-left: 24px;
  }

  .content-blocks li {
    margin-bottom: 1rem;
  }

  /* Trust Section */
  .trust-section {
    background: #2563eb;
    padding: 50px 0;
  }

  .trust-text {
    font-family: 'Work Sans', sans-serif;
    font-weight: 600;
    font-size: 24px;
    color: #ffffff;
    letter-spacing: -0.48px;
    line-height: 1.4;
    margin: 0;
    text-align: center;
  }


  /* Mobile Responsive */
  @media (max-width: 768px) {

    .header-section {
      padding: 40px 0;
    }
    
    .logo-text {
      font-size: 28px;
    }
    
    .nav {
      gap: 16px;
    }
    
    .nav-link {
      font-size: 18px;
    }
    
    .tagline {
      text-align: center;
    }
    
    .tagline p {
      margin: 0 auto;
      max-width: none;
    }

    .form-section {
      padding: 30px 0 60px;
    }

    .form-header {
      margin-bottom: 40px;
    }
    
    .form-headline {
      font-size: 48px;
      letter-spacing: -0.96px;
      text-align: left;
    }
    
    .form-subheadline {
      font-size: 18px;
      letter-spacing: -0.36px;
      margin-bottom: 30px;
      text-align: left;
    }

    .benefit-item {
      font-size: 16px;
    }

    .audit-form {
      padding: 30px 20px;
    }

    .form-grid {
      grid-template-columns: 1fr;
      gap: 20px;
      margin-bottom: 30px;
    }

    .checkbox-group {
      grid-template-columns: 1fr;
      gap: 12px;
    }

    .submit-button {
      font-size: 18px;
    }

    .submit-text {
      padding: 20px 28px;
    }

    .submit-arrow {
      padding: 20px 28px;
      min-width: 50px;
    }

    .additional-content {
      padding: 50px 0;
    }

    .content-blocks {
      font-size: 16px;
    }

    .content-blocks h2 {
      font-size: 28px;
      letter-spacing: -0.56px;
    }

    .content-blocks h3 {
      font-size: 28px;
      letter-spacing: -0.56px;
    }

    .trust-section {
      padding: 40px 0;
    }

    .trust-text {
      font-size: 20px;
      letter-spacing: -0.4px;
    }

</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Form validation and submission handling
    const form = document.getElementById('auditForm');
    
    if (form) {
      form.addEventListener('submit', function(e) {
        // Basic validation
        const requiredFields = form.querySelectorAll('[required]');
        let isValid = true;
        
        requiredFields.forEach(field => {
          if (!field.value.trim()) {
            isValid = false;
            field.style.borderColor = '#ef4444';
          } else {
            field.style.borderColor = '#e5e7eb';
          }
        });

        // Check if at least one service is selected
        const serviceCheckboxes = form.querySelectorAll('input[name="services"]:checked');
        if (serviceCheckboxes.length === 0) {
          isValid = false;
          alert('Please select at least one service you provide.');
          e.preventDefault();
          return;
        }
        
        if (!isValid) {
          e.preventDefault();
          return;
        }
        
        // If valid, let Netlify handle the form submission
        const submitButton = form.querySelector('.submit-button');
        submitButton.textContent = 'Submitting...';
        submitButton.disabled = true;
      });
    }
  });
</script>